
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: sedisamuel
      POSTGRES_PASSWORD: sam182006
      POSTGRES_DB: walletcache
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
